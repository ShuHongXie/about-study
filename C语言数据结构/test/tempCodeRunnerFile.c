if (i < 1 || i > l.length + 1)
  // {
  //   printf("超过index");
  //   return;
  // }
  // if (l.length == MAXSIZE)
  // {
  //   return;
  // }

  // for (j = l.length - 1; j >= i - 1; j--)
  // {
  //   l.elem[j + 1] = l.elem[j];
  // }
  // l.elem[i - 1] = e;
  // ++l.length;